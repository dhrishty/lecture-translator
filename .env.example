# LibreTranslate server with Korean -> English installed.
# Leave blank until a server is set up or selected. No demo fallback.
LIBRETRANSLATE_URL=
# Example for a locally running server: http://127.0.0.1:5000
# This integration does not send API keys or use marketplace credentials.
